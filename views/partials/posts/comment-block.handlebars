<div class="comment">
  <p>{{content}}</p>
  <p>By {{user.username}} on {{created_at}}</p>
  {{#if is_own_comment}}
    <button class="delete-comment-btn" data-comment-id="{{id}}">Delete</button>
  {{/if}}
</div>
